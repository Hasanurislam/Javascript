/*let sum=0;
let n=4;
for(let i=0;i<n;i++)
    {
        sum += (i+1)
    }
    console.log("the sum of "+n+" is " +sum)
    */
   let obj ={
    hasanur:20,
    julfikar:30,
    farid:40,
    sanidul:60

   }
   for(let a of obj)
    {
        console.log(a)
    }